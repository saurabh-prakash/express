---
framework: mocha
postMerge:
  pattern:
    - "./test/*.js"
    - "./test/acceptance/*.js"
preMerge:
  pattern:
    - "./test/*.js"
    - "./test/acceptance/*.js"
preRun:
  command:
    - npm i
postRun:
  command:
    - node --version
version: 2.0
