---
framework: mocha
postMerge:
  pattern:
    - "./test/*.js"
    - "./test/acceptance/*.js"
preMerge:
  pattern:
    - "./test/*.js"
    - "./test/acceptance/*.js"
preRun:
  command:
    - npm i
postRun:
  command:
    - node --version
parallelism: 2
version: 2.1
